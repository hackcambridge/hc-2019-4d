language: node_js

node_js:
  - 8.16

services:
  - postgres

env:
  global:
    NODE_ENV=development
  
before_script:
  - yarn db:migrate

jobs:
  include:
    - stage: test
      script: npm test

notifications:
  email:
    on_success: never
    on_failure: change
