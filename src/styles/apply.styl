/* ==========================================================================
   Apply Section
   ========================================================================== */
.dashboard-section {
  border-bottom: 2px solid darken($backgroundColor, 15%);
  padding: 10px;
  margin-bottom: 10px;
  text-align: left;
}
.logged-in {
  padding: 10px;
  background: $colorSecondary;
  color: white;
}
.status-message {
  display: inline-block;
  padding: 10px 20px;
  border-radius: 5px;
  text-align: center;
  margin-bottom: 15px;
}
.status-message.success {
  // background-color: #C8DCBE;
  border: 2px solid #97DF81;
}
.status-message.warning {
  // background-color: #C8DCBE;
  border: 2px solid #FD8739;
}
.status-message.neutral {
  // background-color: #C8DCBE;
  border: 2px solid $grayLight;
}
.status-message h2 {
  margin-bottom: 0;
}
.status-message span {
  color: $gray;
}