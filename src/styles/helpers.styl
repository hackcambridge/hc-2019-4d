squareify() {
  // HACK: CSS padding always uses width's value for percentages.
  // We can exploit this to turn any element that has a width
  // into a square.
  &::before {
    content: ' ';
    display: block;
    height: 0;
    padding-top: 100%;
  }
}