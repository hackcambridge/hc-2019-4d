/* Variables */

:root {
    --one: 57pt;
    --two: calc(var(--one) * 2);
    --three: calc(var(--one) * 3);
    --four: calc(var(--one) * 4);
    --five: calc(var(--one) * 5);
    --six: calc(var(--one) * 6);
    --seven: calc(var(--one) * 7);
    --eight: calc(var(--one) * 8);
    --nine: calc(var(--one) * 9);
    --ten: calc(var(--one) * 10);
    --eleven: calc(var(--one) * 11);
    --twelve: calc(var(--one) * 12);
    --thirteen: calc(var(--one) * 13);
    --fourteen: calc(var(--one) * 14);
    --gutter: 13pt;
    --half: calc(var(--one) / 2);
}

/* Reset margins and padding */

div {
    margin: 0;
    padding: 0;
}

/* Container */

div.container {
    display: flex;
    flex-direction: column;
}

div.container.a4 {
    position: relative;
    width: 484pt;
    margin: 0 auto;
}

div.container.width {
    margin: 0 var(--one);
}

/* Basic grid */

.row:not(section) {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
}

.persistent.row:not(section) {
    flex-direction: row;
}

.column, .columns {
    margin: 0;
}

.column > *:not(.row) {
    display: block;
}

@media (max-width: 595pt) {
    
    div.container.a4 {
        width: calc(484 / 595 * 100vw);
    }
    .row:not(section) {
        flex-direction: column;
    }
    .column {
        margin-top: var(--half);
    }
    .column:first-child, .empty.column + .column, form.row .column {
        margin-top: 0;
    }
    .container .unit.column {
        width: initial;
        min-width: initial;
    }

}


@media (min-width: 595pt) {

    .columns {
        column-count: 2;
        column-gap: 24pt;
        width: 100%;
        column-fill: auto;
    }
    .columns.balance {
        column-fill: balance;
    }

    /* Rows */
    
    .unit.row:not(section) {
        margin: 0 calc(var(--half) * -1);
    }
    div.row > * {
        margin: 0 var(--half);
    }
    form > .column, .content > .column {
        margin: 0 calc(var(--gutter) / 2);
    }
    form.unit.row, .content.unit.row {
        margin: 0 calc(var(--gutter) / 2 * -1);
    }
    
    .one.unit.row {
        height: var(--one);
    }
    .two.unit.row {
        height: var(--two);
    }
    .three.unit.row {
        height: var(--three);
    }
    .four.unit.row {
        height: var(--four);
    }
    .five.unit.row {
        height: var(--five);
    }
    .six.unit.row {
        height: var(--six);
    }
    .seven.unit.row {
        height: var(--seven);
    }
    .eight.unit.row {
        height: var(--eight);
    }
    .nine.unit.row {
        height: var(--nine);
    }
    .ten.unit.row {
        height: var(--ten);
    }
    .eleven.unit.row {
        height: var(--eleven);
    }
    .twelve.unit.row {
        height: var(--twelve);
    }
    .thirteen.unit.row {
        height: var(--thirteen);
    }
    .fourteen.unit.row {
        height: var(--fourteen);
    }
    
    .one.small.unit.row {
        height: calc(var(--one) - var(--gutter));
    }
    .two.small.unit.row {
        height: calc(var(--two) - var(--gutter));
    }
    .three.small.unit.row {
        height: calc(var(--three) - var(--gutter));
    }
    .four.small.unit.row {
        height: calc(var(--four) - var(--gutter));
    }
    .five.small.unit.row {
        height: calc(var(--five) - var(--gutter));
    }
    .six.small.unit.row {
        height: calc(var(--six) - var(--gutter));
    }
    .seven.small.unit.row {
        height: calc(var(--seven) - var(--gutter));
    }
    
    .one.large.unit.row {
        height: calc(var(--one) + var(--gutter));
    }
    .two.large.unit.row {
        height: calc(var(--two) + var(--gutter));
    }
    .three.large.unit.row {
        height: calc(var(--three) + var(--gutter));
    }
    .four.large.unit.row {
        height: calc(var(--four) + var(--gutter));
    }
    .five.large.unit.row {
        height: calc(var(--five) + var(--gutter));
    }
    .six.large.unit.row {
        height: calc(var(--six) + var(--gutter));
    }
    .seven.large.unit.row {
        height: calc(var(--seven) + var(--gutter));
    }
    
    .half.unit.row {
        height: var(--half);
    }
    .one.half.unit.row {
        height: calc(var(--one) + var(--half));
    }
    .two.half.unit.row {
        height: calc(var(--two) + var(--half));
    }
    .three.half.unit.row {
        height: calc(var(--three) + var(--half));
    }
    .four.half.unit.row {
        height: calc(var(--four) + var(--half));
    }
    .five.half.unit.row {
        height: calc(var(--five) + var(--half));
    }
    .six.half.unit.row {
        height: calc(var(--six) + var(--half));
    }
    .seven.half.unit.row {
        height: calc(var(--seven) + var(--half));
    }
    .eight.half.unit.row {
        height: calc(var(--eight) + var(--half));
    }
    .nine.half.unit.row {
        height: calc(var(--nine) + var(--half));
    }
    .ten.half.unit.row {
        height: calc(var(--four) + ten(--half));
    }
    .eleven.half.unit.row {
        height: calc(var(--eleven) + var(--half));
    }
    .twelve.half.unit.row {
        height: calc(var(--twelve) + var(--half));
    }
    .thirteen.half.unit.row {
        height: calc(var(--thirteen) + var(--half));
    }
    
    /* Columns */
    
    .column {
        flex-grow: 0;
    }    
    .unit.column {
        flex-shrink: 0;
    }
    .unit.column.grows {
        flex-grow: 1;
    }
    .one.unit.column {
        flex-basis: var(--one);
    }
    .two.unit.column {
        flex-basis: var(--two);
    }
    .three.unit.column {
        flex-basis: var(--three);
    }
    .four.unit.column {
        flex-basis: var(--four);
    }
    .five.unit.column {
        flex-basis: var(--five);
    }
    .six.unit.column {
        flex-basis: var(--six);
    }
    .seven.unit.column {
        flex-basis: var(--seven);
    }
    .eight.unit.column {
        min-width: var(--eight);
    }
    .nine.unit.column {
        flex-basis: var(--nine);
    }
    .ten.unit.column {
        flex-basis: var(--ten);
    }
    .eleven.unit.column {
        flex-basis: var(--eleven);
    }
    .twelve.unit.column {
        flex-basis: var(--twelve);
    }
    .thirteen.unit.column {
        flex-basis: var(--thirteen);
    }
    .fourteen.unit.column {
        flex-basis: var(--fourteen);
    }

    .small.unit.column {
        flex-basis: var(--gutter);
    }
    .one.small.unit.column {
        flex-basis: calc(var(--one) - var(--gutter));
    }
    .two.small.unit.column {
        flex-basis: calc(var(--two) - var(--gutter));
    }
    .three.small.unit.column {
        flex-basis: calc(var(--three) - var(--gutter));
    }
    .four.small.unit.column {
        flex-basis: calc(var(--four) - var(--gutter));
    }
    .five.small.unit.column {
        flex-basis: calc(var(--five) - var(--gutter));
    }
    .six.small.unit.column {
        flex-basis: calc(var(--six) - var(--gutter));
    }
    .seven.small.unit.column {
        flex-basis: calc(var(--seven) - var(--gutter));
    }
    
    .large.unit.column {
        flex-basis: var(--gutter);
    }
    .one.large.unit.column {
        flex-basis: calc(var(--one) + var(--gutter));
    }
    .two.large.unit.column {
        flex-basis: calc(var(--two) + var(--gutter));
    }
    .three.large.unit.column {
        flex-basis: calc(var(--three) + var(--gutter));
    }
    .four.large.unit.column {
        flex-basis: calc(var(--four) + var(--gutter));
    }
    .five.large.unit.column {
        flex-basis: calc(var(--five) + var(--gutter));
    }
    .six.large.unit.column {
        flex-basis: calc(var(--six) + var(--gutter));
    }
    .seven.large.unit.column {
        flex-basis: calc(var(--seven) + var(--gutter));
    }

    .half.unit.column {
        flex-basis: var(--half);
    }
    .one.half.unit.column {
        flex-basis: calc(var(--one) + var(--half));
    }
    .two.half.unit.column {
        flex-basis: calc(var(--two) + var(--half));
    }
    .three.half.unit.column {
        flex-basis: calc(var(--three) + var(--half));
    }
    .four.half.unit.column {
        flex-basis: calc(var(--four) + var(--half));
    }
    .five.half.unit.column {
        flex-basis: calc(var(--five) + var(--half));
    }
    .six.half.unit.column {
        flex-basis: calc(var(--six) + var(--half));
    }

}
