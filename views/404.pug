extends layouts/a4
block mainContent
  - title = 'Not Found'
  section(grid='rows')
    h1(rows='1.0')
      span.pixel Error 404
      |
      |
      = title
    p(rows='1.0')
      | Sorry, there doesn’t appear to be a page at this location.
      br
      | Think this is wrong? 
      a(href='mailto:team@hackcambridge.com?subject=Missing page') Let us know
