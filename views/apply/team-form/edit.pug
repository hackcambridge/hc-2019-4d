extends ../../layouts/a4
block mainContent
  - title = 'Team Application'
  if error
    section(color='red' grid='rows')
      p(rows='0.5')= error
  section
    form(grid='rows')
      h1(rows='1.0') Team Application
      p(rows='0.5') Enter your teammates’ application IDs to update your team.
      include team-form
      div(rows='0.5')
        button(data-method='PATCH' data-action='/apply/team') Submit
