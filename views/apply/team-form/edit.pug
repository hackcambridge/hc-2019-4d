extends ../../layouts/a4
block mainContent
  - title = 'Team Application'
  if error
    section(color='red' grid='rows')
      p(rows='0.5')= error
  section
    form(grid='rows' action='/apply/team' method='post')
      h1(rows='1.0') Team Application
      p(rows='0.5') Enter your teammates’ application IDs to update your team.
      include team-form
      div(rows='0.5')
        input(type='hidden' name='_method' value='patch')
        button(type='submit') Submit
